[package]
name = "highly_sendable"
version = "0.2.0"
edition = "2024"
license = "MIT OR Apache-2.0"
description = "Mostly objects that can assist with communicating progress and status within your programme and beyond."
repository = "https://github.com/coruscateor/highly_sendable"
homepage = "https://coruscateor.com/projects/highly-sendable"
keywords = ["send", "result", "text", "progress", "status"]
authors = ["Paul Saunders"]
rust-version = "1.95.0"

[dependencies]
delegate = "0.13.5"
serde = { version = "1.0.228", features = ["derive"], optional = true }
corlib = { version = "0.5.0", optional = true }
accessorise = "0.2.0"
pastey = "0.2.2"
inc_dec = "0.1.0"

bytes = { version = "1.12.1", optional = true }

[package.metadata.docs.rs]
all-features = true
rustdoc-args = ["--cfg", "docsrs"]

